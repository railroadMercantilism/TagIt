import SwiftUI
import SwiftData

import SwiftUI
import UserNotifications

struct ContentView: View {
    var body: some View {
        TagList()
            .frame(minWidth: 700, minHeight: 300)
    }
}

#Preview {
    ContentView()
}

